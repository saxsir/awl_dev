!!!
%html
  %head
    %title AwlDev
    %meta{ :name => "viewport", :content => "width=device-width, initial-scale=1.0" }
    = csrf_meta_tags
    = stylesheet_link_tag "application", :media => "all"
    = yield :head
  %body#home
    = render 'layouts/fb-root'
    %header
      #cover
        #bg
          #welcome
            .container
              .row
                .span4.offset1
                  %h1>
                    = image_tag('http://cambelt.co/120x120/Logo?color=25d54e', :alt => 'AWL')
                    AWL
                .span7
                  %h2#awl-phrase>
                    クリエイターをもっと自由に。マニアを共犯者に。
                  .row
                    .span8.offset1
                      %p#awl-desc
                        AWLは、クリエイターとマニアをつなぐ、サブカルチャーに
                        %br
                        特化したクラウドファンディングサイトです。
      #navigation
        .navbar.navbar-static-top.affix-top{"data-spy" => "affix", "data-offset-top" => "430"}
          .navbar-inner
            .container
              %a.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
                %span.icon-bar
                %span.icon-bar
                %span.icon-bar
              .nav-collapse
                %ul.nav.pull-right
                  %li
                    .icon.about
                      - #空div
                    = link_to('About', about_path)
                  %li
                    .icon.project
                      - #空div
                    = link_to('Project', projects_path)
                  -if user_signed_in?
                    %li
                      .icon.mypage
                        - #空div
                      = link_to('MyPage', mypage_index_path(:username => current_user.username))
                    %li#last
                      .icon.login-out
                        - #空div
                      = link_to('Logout', destroy_user_session_path, :method => :delete)
                  -else
                    %li
                      .icon.login-out
                        - #空div
                      %a#nav_login_home{"data-toggle" => "popover"}
                        Login
                    %li#last
                      .icon.signup
                        - #空div
                      = link_to('Signup', new_user_registration_path)

    .container.main
      = yield
    = render 'layouts/footer'
    = javascript_include_tag "application"
    = yield :javascript

